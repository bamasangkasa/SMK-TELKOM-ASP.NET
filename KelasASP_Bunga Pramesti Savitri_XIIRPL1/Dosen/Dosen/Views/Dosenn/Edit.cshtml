@model Dosen.Dosen

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Dosen</legend>

        @Html.HiddenFor(model => model.DosenID)

        <div class="editor-label">
            @Html.LabelFor(model => model.NamaDosen)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.NamaDosen)
            @Html.ValidationMessageFor(model => model.NamaDosen)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.KodeDosen)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.KodeDosen)
            @Html.ValidationMessageFor(model => model.KodeDosen)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
