@model IEnumerable<CRUD2tabel.Models.FakultasModels>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
<p> 
    @using (@Html.BeginForm())
    {
        <label> Search </label>
        <input type="text" name="searchFakultas"/>
        <input type="submit" value="Go">
    }


<p>
<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Fakultas)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Fakultas)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_fakultas }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_fakultas }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_fakultas })
        </td>
    </tr>
}

</table>
